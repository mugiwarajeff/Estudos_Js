/*
    em algumas linguagem precisamos dizer qual o tipo da nosssa variavel, uma string, um int e etc
    isso não acontece no JS, vez que ela apresenta tipagem dinamica/fraca, ou seja, seus tipos são gerados automaticamente

    podemos usar a função build in chamada tipeof para checar esse tipo
*/

let a = "jeffin"
let b = 20

console.log(typeof a)
console.log(typeof b)