//podemos usar os blocos if sem os blocos de codigo, porém isso executará apenas uma sentença de codigo, então também não devemos usar ; no fim do if, pois isso executará uma sentença vazia 


function notaBoa(nota) {
    if (nota > 7);
    console.log(nota)
}

notaBoa(5)